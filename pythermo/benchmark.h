#ifndef THERMO_UTILS_GAS_STATE_H
#define THERMO_UTILS_GAS_STATE_H

#include <libthermo/gas.h>

#include <memory>


namespace thermo::utils
{
    // void Benchmark(ThermoInterface* gas, long ntimes);
}
#endif